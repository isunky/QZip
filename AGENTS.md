# 验证与交付

- 按改动风险选择验证范围；报告实际执行的检查、结果及未验证项。
- 测试断言通过但存在未处理异常或非零退出码时，不得报告检查通过。
- 修改文件关联、窗口显示或单实例逻辑后，验证真实双击启动和已有实例接收文件；未验证时明确说明。
- 区分本地检查、目标平台 CI、安装包运行和发布资产验证，不得互相替代。
- 用户要求“简单验证”时执行相关检查并报告边界；要求等待 CI 完成时才持续等待。用户叫停后停止等待。
- 验证不自动授权推送、发布、覆盖用户文件或修改系统文件关联。
