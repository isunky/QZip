# 轻压 · QZip 产品需求（V2.1）

> 来源：`QZip_PRD_V2.1.md`。本文件保留产品、体验、架构与验收要求；实施顺序、任务拆分和完成状态见 `QZip_TODO_V2.1.md`。
>
> 产品名称：轻压 · QZip；用户界面英文品牌：QZip；版本范围：V1.0。

## 1. 范围与约束

### 1.1 产品目标

QZip 是本地优先、跨平台、轻量化的桌面压缩软件。用户应能以拖拽和合理默认值，快速、安全地完成压缩、解压和压缩包浏览。

V1.0 必须支持：

- 创建：7Z、ZIP、TAR、TAR.GZ、TAR.XZ；
- 读取、浏览和解压：7Z、ZIP、RAR、TAR、GZ、XZ、BZ2；
- 部分解压、密码压缩/解压、7Z 文件名加密、完整性测试；
- 文件/文件夹拖拽、任务进度、取消、失败重试；
- 浅色、暗夜、跟随系统和 6 套主题色；
- 文件关联、高频右键菜单和 Windows/macOS/Linux 基础发行；
- 合规分发的 7-Zip Sidecar 与可替换的 `ArchiveBackend` 接口。

V1.0 明确不包含：RAR 创建、修复压缩包、自解压 EXE、云服务、账号、广告、插件市场、完整文件管理器、远程挂载、跨重启任务恢复、移动端及企业授权服务器。

### 1.2 强制工程原则

- 安全与隐私、数据契约、当前里程碑验收、页面功能、UI 规范、性能、可选优化，按此优先级处理冲突。
- UI、应用服务、压缩后端必须解耦；React 组件不得拼接或执行 7-Zip 命令，也不得解析 stdout。
- Sidecar 必须以参数数组执行，禁止 Shell 字符串和 `cmd /C`。
- 不得伪造压缩结果、进度或平台集成；新增核心逻辑必须有测试。
- 密码不得进入日志、配置、事件、任务历史、LocalStorage 或错误报告。
- 默认本地运行：不上传文件、不引入账号、广告或默认开启的遥测。

### 1.3 固定工程标识

| 项目 | 固定值 |
|---|---|
| 应用显示名 | `QZip` |
| 中文正式名 | `轻压 · QZip` |
| Bundle Identifier | `app.qzip.desktop` |
| Rust Workspace | `qzip` |
| 前端包作用域 | `@qzip/*` |
| 配置目录 / 环境变量前缀 | `qzip` / `QZIP_` |
| CSS Token 前缀 | `--qzip-` |
| CLI | `qzip-cli` |

## 2. 用户与核心流程

| 场景 | 输入 | 必须结果 |
|---|---|---|
| 快速压缩 | 单个/多个普通文件或文件夹 | 进入创建压缩包页；多对象创建一个压缩包 |
| 快速解压 | 单个压缩包 | 进入快速解压页 |
| 批量解压 | 多个压缩包 | 创建多个解压任务 |
| 混合拖拽 | 普通对象与压缩包混合 | 让用户选择处理方式 |
| 浏览 | 双击或打开压缩包 | 进入压缩包浏览页 |
| 部分解压 | 浏览页选择条目 | 创建选中项解压任务 |
| 完整性测试 | 选择测试 | 创建测试任务 |
| 密码错误 | 失败任务重试 | 保留非敏感参数，要求重新输入密码 |

高频任务的体验标准：首次打开即可理解；普通压缩最多两次点击开始；普通解压一次确认；失败提示说明原因、下一步、是否可重试，并提供技术详情入口。

## 3. 功能需求

### FR-01 首页与拖拽

- 显示插画、主标题“将文件拖到这里”、副标题、压缩文件、打开压缩包、格式提示、任务与设置入口。
- 拖拽识别在后台进行，不阻塞 UI；激活态使用柔和边框/背景变化，显示对应操作提示，不使用强虚线框。
- 拖入后在当前窗口切换页面，不弹出传统参数窗口。

### FR-02 创建压缩包

- 显示输入摘要、名称、保存位置、格式、压缩方式、密码、开始操作与“更多设置”。
- 默认：单文件夹用文件夹名，单文件用去扩展名，多文件用“压缩文件-日期时间”；首次格式为 7z；压缩方式为 Balanced；输出在源目录；重名自动追加序号；不得直接覆盖。
- 更多设置包含分卷、文件名加密、Solid、压缩后测试、删除源文件、排除规则；线程、字典和算法只在专家模式显示。
- 格式切换后按能力动态禁用选项；创建立即返回任务 ID，页面不得等待完成；删除源文件必须二次确认。

### FR-03 快速解压与安全

- 显示格式、压缩大小、预计解压大小、文件数量、输出位置、冲突策略、密码及查看内容入口。
- 默认输出到同名目录，冲突策略为 `Rename`，默认不创建链接；风险检查必须早于写文件。
- 普通压缩包可一键解压；密码错误可原地重试；不得静默覆盖。

### FR-04 压缩包浏览

- 工具栏提供返回、添加、解压、测试、更多；包含面包屑和搜索。
- 默认列为名称、大小、类型、修改时间；支持目录双击、面包屑、多选、即时过滤、部分解压、完整性测试和属性查看。
- 大列表必须虚拟化；加载、搜索和极大压缩包分批载入不得阻塞界面。

### FR-05 任务中心

- 包含进行中、已完成、失败三类任务。
- 进行中显示文件名、状态、进度、当前文件、速度、剩余时间、大小与取消；未实现真实暂停前，暂停必须隐藏或禁用并说明。
- 已完成支持打开结果、打开位置、再次执行；失败任务提供原因、修复操作、重试与技术详情。
- 重试保留输入、输出、格式、压缩配置、冲突策略和选中条目；不得保留密码、失效临时路径或原进程 ID。

### FR-06 设置与本地存储

- 设置分类：常规、压缩、解压、文件关联、外观、隐私与安全、更新、关于。
- 外观必须支持浅色、暗夜、跟随系统、Mint/Ocean/Lavender/Amber/Coral/Cyan Slate、列表密度、减少动态和 90%/100%/110%/125% 缩放。
- 持久化主题、缩放、默认格式、压缩预设、冲突策略及解压偏好；不得保存密码、完整命令或未脱敏诊断数据。
- 任务历史可保存显示名、状态、时间、输出位置和错误代码，默认不保存压缩包内部清单。

### FR-07 系统集成与发行

- 文件关联：7z、ZIP、RAR、TAR、GZ、XZ、BZ2；双击复用单实例并按需请求密码。
- 右键菜单：使用 QZip 打开、压缩为 `.7z` / `.zip`、解压到当前目录、解压到同名目录、更多选项；低频项进入二级菜单。
- Windows 支持 Explorer、长路径、保留名称与 100%–200% DPI；macOS 支持 Finder Quick Action、签名和 Gatekeeper；Linux 支持 MIME、Desktop Entry、AppImage、Wayland/X11。
- 发布物使用 `QZip-{version}-{platform}-{arch}` 命名，并包含校验和、第三方许可、Sidecar 版本和来源信息。

## 4. 领域、接口与后端要求

### 4.1 领域规则

- 格式枚举至少覆盖 7Z、ZIP、TAR、TAR.GZ、TAR.XZ、RAR、GZ、XZ、BZ2、ISO、CAB、WIM、Unknown；`Unknown` 不得创建任务，RAR 在 V1.0 只读。
- 压缩预设：Store、Fast、Balanced、Small、Maximum；默认 UI 仅展示 Fast、Balanced、Small。
- 冲突策略：Ask、Rename、Overwrite、Skip；默认 `Rename`。
- 任务类型：Create、Extract、List、Test、Update；状态：Queued、Scanning、Running、Cancelling、Completed、Failed、Cancelled。
- 创建请求必须校验非空输入、输出不位于输入目录内、后端能力、删除源文件二次确认；密码使用受控的秘密类型且不得持久化。

### 4.2 后端与 Command 契约

- 实现 `ArchiveBackend`：能力探测、列表、创建、解压、测试；应用通过 `BackendRegistry` 选择读写后端，UI 不得假设只有一个后端。
- V1.0 默认注册 `SevenZipCliBackend`；其职责包括 Sidecar 定位、能力探测、参数映射、进程控制、输出解析、退出码映射与取消。
- 前端仅调用结构化 Command：能力探测、格式识别、输入扫描、创建/解压/测试任务、列表、取消、重试、任务查询、设置、打开/定位路径。
- `TaskCreatedResponse` 返回 `taskId` 与 `queued/scanning` 状态；任务事件和进度必须结构化，未知进度使用不确定进度，事件节流建议 100–250ms。
- 统一错误 DTO 必须覆盖文件、权限、占用、磁盘、密码、损坏包、格式、后端、选项、不安全路径、炸弹风险、取消、请求和未知错误；常规 UI 默认隐藏技术详情。

### 4.3 任务状态机

`Queued → Scanning → Running → Completed/Failed/Cancelling → Cancelled`。失败、已取消、已完成可通过创建新的运行实例回到 Queued；禁止 Completed 直接 Running、Failed 直接 Running 和同一任务绑定两个后端进程。

取消时必须终止子进程、等待结束、清理未完成文件、广播最终状态；无法清理时保留警告并提供打开所在位置。

## 5. 安全、隐私与兼容性

### 5.1 解压安全

- 默认拒绝符号链接与硬链接；限制条目数、总大小、单文件大小、目录深度和压缩比。
- 每个输出条目必须拒绝绝对路径、盘符、UNC；规范化 `.`/`..`；验证最终路径仍位于输出目录；检查 Windows 保留名称、尾部空格/句点与链接策略。
- 压缩炸弹风险（数量、尺寸、比例、深度、空间）触发后必须中断或暂停、说明风险、提供取消；不得静默忽略。
- 日志只记录必要且脱敏的信息；路径诊断必须用户主动导出，密码永不导出。遥测默认关闭。

### 5.2 兼容性

测试必须覆盖中文、日文、韩文、Emoji、历史 ZIP 编码、超长路径、空目录、隐藏/只读文件、软链接、大小写冲突、保留名称、文件占用、磁盘不足、超过 4GB 文件、十万级文件、分卷、密码包和损坏包。

## 6. UI、无障碍与响应式要求

- 视觉方向：现代、轻量、温和、清晰、克制、可靠；暖白/浅灰底，主题色仅作强调，不使用大面积霓虹、重毛玻璃、传统压缩软件工具栏或网页后台式布局。
- Token 分层：基础色 → 语义色 → 组件 → 页面；组件不得散落硬编码主题色。浅色、暗夜和 6 套主题都必须完整可用，语义色含义不得因主题变化。
- 默认窗口 `1080×720`，最小 `760×520`，标题栏建议 56px；760–899px 不得出现横向滚动。
- 必备组件：Button、Segmented Control、Input、Card、Progress、Table、Toast；所有交互组件具备 default/hover/pressed/focus/disabled/loading 或相应状态。
- 动效 80–240ms，使用 `cubic-bezier(0.2, 0, 0, 1)`，支持减少动态；禁止夸张弹跳、粒子和文件飞行动画。
- 核心功能键盘可达，焦点顺序遵循视觉顺序，焦点 2px 主题色边框；正文对比度至少 4.5:1；图标按钮有可访问名称和 Tooltip；最小点击区 32×32px。

## 7. 性能、质量与发布验收

- 目标：冷启动尽量小于 1s、首页可交互尽量小于 1.5s、空闲内存尽量小于 100MB；主线程不扫描/压缩，大列表虚拟化，进度节流，扫描可取消，任务异常不崩溃窗口。
- Rust 测试覆盖参数映射、路径、错误、状态机、能力、输出、配置迁移；集成测试覆盖创建、解压、列表、测试、密码、取消、子进程异常与磁盘/占用异常；安全测试覆盖 Zip Slip、路径、链接、炸弹、注入、恶意文件名和泄露。
- 前端测试覆盖拖拽、创建表单、能力禁用、任务状态、错误重试、主题、暗夜、焦点与窄屏。
- CI/PR 必须通过格式、类型、Lint、前端测试、Rust 测试、Clippy、安全子集、构建、许可证及密钥检查。发布构建必须生成校验和、许可、Sidecar 文件校验和版本记录。

## 8. V1.0 验收摘要

功能、体验、安全、发布四类验收必须同时成立：可创建常用格式、解压并浏览常用包、支持密码/文件名加密/分卷/取消/测试/拖拽和系统集成；流程简单、主题完整、键盘可用、最小窗口不溢出；路径/密码/炸弹/覆盖/取消清理安全；三平台产物、许可、隐私说明、卸载说明、已知问题和 GitHub Release 齐备。
