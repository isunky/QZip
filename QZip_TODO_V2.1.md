# 轻压 · QZip 实施 TODO（V2.1）

> 唯一需求来源：`QZip_PRD_V2.1.md`。本文件只记录实施顺序、完成状态和验收阻塞项。
>
> 规则：按里程碑推进；未通过对应验收不得提前开发后续功能；所有涉及密码、路径、Sidecar、任务状态或数据契约的任务必须附测试。
>
> 状态说明：`[x]` 已完成并通过对应验收；`[~]` 已实现第一版，仍有验收或范围缺口；`[ ]` 未实施。

## 当前状态

- [x] **M0** 仓库、设计系统与静态首页已完成并通过 `pnpm check`、Tauri Debug 构建和设计 QA。
- [x] **M1** 后端技术验证已实现并通过 7Z/ZIP/RAR、Unicode、密码、损坏包、取消和打包验证。
- [~] **M2–M5** 已完成 RC2 高影响体验改进：原生拖放、批量解压、能力动态禁用、分页搜索、部分解压、任务详情及结果入口已接入；可取消深度扫描、虚拟列表和完整无障碍验收仍须在 V1.0 前关闭。
- [~] **M6** 已完成设置核心、设置页、单实例路由，以及 NSIS 可注册/卸载的 Windows 11 稀疏 MSIX Shell 集成；本机 NSIS 安装、应用启动、原生窗口关闭、默认应用能力注册和真实 Explorer 一级菜单已验收，干净 Windows 环境的升级、卸载与 DPI 证据待完成。
- [~] **M7** 已实施 RC1 的安全写入、Sidecar 完整性、Windows 发布物组装、开发签名 Shell 包与严格签名门禁；性能基线已记录，干净安装验证和受信任签名仍未完成。

### 版本边界

- **RC1**：Windows x64 公开预览；可以携带已完整披露的功能缺口，但安全、安装和发布门禁不得降级。
- **RC2**：补齐 M2～M5 的 Windows V1.0 功能与验收缺口。
- **V1.0**：Windows 10/11 x64 稳定版；PRD 中全部 Windows 功能、体验、安全和发布验收均须通过。
- **V1.1**：实现 macOS/Linux 发行、系统集成、签名和平台验收，不阻塞 Windows V1.0。

### RC1 实施记录（2026-07-28）

- [x] 已通过 `pnpm check`、`pnpm sidecar:test`、`pnpm tauri:build:debug` 与 Windows x64 NSIS/MSI 打包。
- [x] 已在本地组装 `1.0.0-rc.1` 的 NSIS、MSI、便携 ZIP、SHA-256 清单和 Sidecar 复核清单；发布物组装脚本已验证通过。
- [x] 已验证未签名的本地资产会被 `-RequireTrustedSignature` 严格拒绝，不能绕过公开发布门禁。
- [x] 已修复 NSIS Shell 资源路径、稀疏 MSIX 外部内容/`desktop5` 清单和 COM DLL 导出；本机注册包 `app.qzip.desktop.shell 1.0.0.2` 状态为 `Ok`。
- [x] 已在本机 Windows 11 Explorer 的文件及文件夹真实右键菜单中确认一级“QZip”入口，并在文件菜单中确认打开、压缩为 7Z/ZIP、解压到当前目录/同名目录和更多选项六个子命令；修复后 NSIS 安装包已重新构建。
- [~] GitHub 仓库尚未配置受信任证书机密，且未创建 `v1.0.0-rc.1` 标签或 GitHub prerelease。
- [x] 已记录 Windows RC1 真实启动、内存、十万项大包首批列表、进度突发与异常恢复基线；第三方压缩软件交叉兼容性不属于 RC1 发布门禁。

### RC1 核心流程人工验收（2026-07-29）

- [x] 已由用户在本机 Windows 环境验证：从界面创建压缩包、打开/快速解压压缩包及任务启动的核心流程可用。
- [x] 已生成包含本次修复的 NSIS 安装包；当前用户开发证书已安装，Shell DLL 的开发签名验证通过。
- [~] 本次人工验收不替代取消、错误密码、风险包、磁盘不足、文件占用、临时文件清理及干净环境的专项验收。

### 核心流程自动化回归（2026-07-29）

- [x] Vitest 已覆盖连续点击创建、任务取消按钮和错误密码任务卡重试。
- [x] `task-runtime` 已覆盖取消清理、错误密码重试、创建/解压冲突、Windows 文件占用、非法状态、终态清理和最多 2 个并发任务。
- [x] Windows Sidecar 回归已覆盖错误密码解压、正确密码重试、7Z/ZIP/TAR.GZ/TAR.XZ、RAR、Unicode 路径和临时目录残留检查。
- [~] 压缩炸弹、磁盘不足、链接和完整恶意路径矩阵仍属于 R1-02 后续专项。

### RC2 功能体验实施记录（2026-07-30）

- [x] 版本提升为 `1.0.0-rc.2`；Windows 安装包内部产品版本继续使用 MSI 兼容的 `1.0.0`。
- [x] Windows 原生拖放会识别压缩包、普通路径和混合内容；单包进入快速解压，多包进入批量解压，普通或混合内容进入创建页。
- [x] 批量解压默认输出到各压缩包的同名文件夹；加密包和需要风险确认的包会跳过并提示单独处理。
- [x] 创建格式根据当前后端能力动态禁用；损坏包和不支持格式不再错误弹出密码输入框。
- [x] 压缩包浏览接入服务端搜索和每页 500 项追加加载，任务卡增加技术详情、错误代码、警告及失败目标位置入口。
- [x] `pnpm check`、Windows 核心回归和 RC2 release/NSIS 构建通过；当前安装包 SHA-256 为 `393A87FD46D02EF35D154DDE63B525D90B721FEB7D773DE704C948C59BB4E1F5`。

### NSIS 文件关联安装体验（2026-08-12）

- [x] NSIS 新增“文件关联”安装步骤，可逐项选择 7Z、ZIP、RAR、TAR、GZ、TGZ、XZ、TXZ、BZ2、ISO、CAB 与 WIM；全新安装默认勾选 7Z、ZIP、RAR。
- [x] 选择结果写入当前用户注册表，覆盖安装会保留原选择；取消勾选或卸载时会恢复安装前由 QZip 接管的扩展名默认值并清理能力注册。
- [x] 安装器只注册 `Capabilities`/`RegisteredApplications`，不修改 Windows 受保护的 `UserChoice`；需要用户确认的格式由系统设置完成。安装完成页不再自动提供打开系统默认应用设置的选项。
- [x] 应用设置页改为读取实际文件关联能力注册状态，并可直达 QZip 的 Windows 默认应用设置页。
- [x] 自定义中英文 NSIS 模板已通过真实打包；已目视确认中文文件关联页的标题、说明、全选和 12 个格式标签正常显示。开发签名构建产物为 `target/release/bundle/nsis/QZip_1.0.0_x64-setup.exe`，SHA-256 为 `2FE4DF31C709A375BCBF6324B07E8AC4481DF1EAF0061865C72924BC85C707D8`。升级保留和卸载恢复仍需人工安装验收。

## M0：仓库和设计系统（完成）

- [x] 建立 pnpm Monorepo、Rust Workspace、Tauri 2 桌面壳与五个领域 crate。
- [x] 配置 `QZip`、`app.qzip.desktop`、Node/Rust 工具链、许可证与 GitHub Actions。
- [x] 建立 `@qzip/ui` Token、基础组件、浅色/暗夜和 6 套主题。
- [x] 实现 PIC 参考图风格的首页、标题栏、任务空态、设置面板、主题切换与最小窗口布局。
- [x] 生成应用图标，完成首页视觉对照报告。

**验收记录**：`design-qa.md`；`pnpm check`；`target/debug/qzip-desktop.exe`。

## M1：后端技术验证

### M1-A 领域与后端接口

- [x] M1-01 在 `archive-core` 定义格式、预设、冲突策略、操作、任务状态、请求/结果、错误和能力模型。
- [x] M1-02 定义 `ArchiveBackend` 与 `BackendRegistry`；确保 `archive-core` 不依赖 Tauri 或 7-Zip。
- [x] M1-03 定义能力、进度、错误 DTO 与对应 TypeScript 类型；M2/M3 再接入创建/解压 Tauri Command。
- [x] M1-04 为领域模型、路径安全、参数映射、输出解析、错误映射和取消行为编写自动化测试。

### M1-B SevenZipCliBackend

- [x] M1-05 建立 Sidecar 目录、目标三元组命名、版本记录、官方 MSI/源码来源、SHA-256 和许可证文件。
- [x] M1-06 实现 Sidecar 定位与启动前能力/版本探测；Windows 打包前校验目标二进制与源码存在。
- [x] M1-07 以 `Command::new(...).arg(...).args(...)` 实现进程启动；禁止 Shell 字符串。
- [x] M1-08 建立集中式版本检查、参数映射、输出解析、列表解析和错误映射。
- [x] M1-09 已实现创建 7Z/ZIP、解压 7Z/ZIP/RAR、列表、测试和取消，并完成真实样本验证。
- [x] M1-10 建立开发者专用 `qzip-cli` 受控验证入口。

### M1-C M1 验收

- [x] M1-11 中文、空格和 Unicode 路径成功；所有操作不经 Shell。
- [x] M1-12 密码、完整命令和敏感文件清单不进入日志/事件/历史。
- [x] M1-13 成功、密码错误、损坏包、磁盘不足、未知退出码均映射为结构化错误。
- [x] M1-14 参数映射、输出解析和取消行为具有自动化测试。

## M2：创建压缩包

- [~] M2-01 已实现 Windows 原生文件/文件夹拖放识别和异步输入分类；超大目录的深度扫描进度与取消仍待补。
- [~] M2-02 已实现创建压缩包页面：输入列表、名称/位置、7Z/ZIP/TAR/TAR.GZ/TAR.XZ、压缩预设、密码和完整性测试选项；选择输入或切换格式时会即时生成输出名。删除源文件因运行时尚未支持而不在 RC1 UI 中暴露；完整交互验收待补。
- [~] M2-03 已在运行时拒绝覆盖既有输出、拒绝输入目录内递归输出，并以同目录随机暂存目录提交；TAR.GZ/TAR.XZ 的内部 TAR 将使用最终文件名，避免暂存名泄漏。序号避重名 UI 待补。
- [x] M2-04 已基于后端能力动态禁用不可写格式和密码入口，其他格式及加密文件名继续通过“更多设置”渐进展示。
- [~] M2-05 已通过异步 Tauri Command 创建任务并订阅实时事件，创建页不等待任务完成；任务提交在缺少运行环境时会返回错误而非退出进程，且本机界面创建流程已人工验收。待补底部任务条和 UI 自动化验收。
- [~] M2-06 已实现压缩后测试选项；删除源文件将留待具备二次确认与可恢复机制后再启用。
- [x] M2-07 已完成 7Z/ZIP/TAR/TAR.GZ/TAR.XZ 的真实 7-Zip 创建、列出、完整性测试、解压和哈希一致性回归，并通过连续点击、取消和 UI 非阻塞自动化回归。

## M3：快速解压和安全

### M3-A 解压体验

- [x] M3-01 已实现压缩包格式识别、摘要、快速解压和同名目录默认输出，并完成界面核心流程人工验收。
- [x] M3-02 已实现冲突策略、密码输入、查看内容入口和任务创建；仅 `WRONG_PASSWORD` 弹出密码框，任务卡支持原地重新输入密码并已有自动化测试。
- [x] M3-03 已支持单包快速解压、多包多任务解压，以及混合拖放进入创建流程；批量流程会跳过需密码或风险确认的包。

### M3-B 安全服务

- [x] M3-04 已实现并测试路径规范化、绝对路径/盘符/UNC/Zip Slip/保留名/尾随空格句点检查。
- [x] M3-05 已在预检中拒绝符号链接和硬链接；最终输出路径会拒绝重解析点，并先解压到同卷暂存目录后安全合并。
- [x] M3-06 已实现条目数、尺寸、深度和压缩比限制及一次性风险确认，并在解压前检查可用磁盘空间。
- [x] M3-07 创建和解压的取消/失败均清理受控临时输出；清理失败会写入任务告警，并通过取消、错误密码、冲突和 Windows 文件占用回归。
- [~] M3-08 已覆盖递归输出、既有输出、重命名/覆盖/跳过冲突、错误密码和取消；风险包及完整恶意路径集成矩阵待补。

## M4：压缩包浏览

- [x] M4-01 已实现浏览页工具栏、面包屑、后端搜索、默认列和压缩包状态栏。
- [x] M4-02 已实现多选、目录导航、属性查看、将所选项传入部分解压和完整性测试任务。
- [~] M4-03 后端和前端已支持每页最多 500 项的分页与追加加载，并完成十万项大包首屏性能基线；完整虚拟列表仍待补。
- [ ] M4-04 完成暗夜与窄窗口的可读性、键盘和选择状态测试。

## M5：任务中心

- [x] M5-01 已实现最多 2 并发的 `task-runtime`、状态机、进度事件、取消、100 条本地历史和重试参数克隆，并通过状态机与并发自动化回归。
- [x] M5-02 已实现进行中、已完成、失败视图，以及任务 ID、操作、目标位置、错误代码和警告详情。
- [x] M5-03 已实现取消、重试、清理已完成、打开结果、打开位置和技术详情入口。
- [x] M5-04 已确保密码不进入任务快照/本地历史，重启后的历史记录强制不可重试，并通过密码脱敏和清理回归；临时路径与进程 ID 的专项测试待补。
- [x] M5-05 已覆盖非法状态跳转、最多 2 并发、错误密码失败修复、取消终态和已完成任务清理。

## M6：设置与系统集成

- [~] M6-01 实现 `AppSettings` 持久化、默认值、迁移和“不得持久化密码”单元测试；需补充损坏 Store 文件端到端测试。
- [x] M6-02 完成所有设置页：常规、压缩、解压、外观、隐私安全、更新、关于。
- [x] M6-03 持久化主题、缩放、密度、减少动态与压缩/解压偏好。
- [x] M6-04 实现单实例、双击打开路由和系统通知；NSIS 提供 12 种格式的逐项关联选择、全选、全新安装默认值与升级选择保留，并注册对应独立 ProgID、`Capabilities` 和 `RegisteredApplications`。Windows 受保护的默认应用选择仍由用户在系统设置中确认。
- [x] M6-05 已实现 Windows 右键高频操作 DLL、受限 token 交接、`desktop5` 稀疏 MSIX 清单、标准 COM 导出和 NSIS 注册/卸载钩子；已在真实 Windows 11 Explorer 验收文件及文件夹的一级 QZip 菜单，并在文件菜单中确认六个子命令可见。MSI 仍不提供现代右键菜单，作为 RC1 已知问题披露。
- [~] M6-06 已通过本机 NSIS 安装、Shell 包注册、文件关联能力注册、应用启动/关闭和真实 Explorer 右键菜单验收；干净 Windows 环境的升级、卸载、文件关联清理、长路径及 100%/150%/200% DPI 验收待完成。

## M7：发布候选

- [x] M7-01 RC1 不要求第三方压缩软件交叉兼容性认证；保留现有合成样本仅用于 QZip/Sidecar 核心回归，不对外宣称 WinRAR、Bandizip、PeaZip、Keka 等兼容认证。
- [x] M7-02 已完成性能基线：Windows 11 实际 release 构建连续 5 次首页可交互中位数 981ms、空闲工作集约 28.3MB；十万项合成 7Z 首批列表 1353ms；损坏包错误呈现后主窗口仍存活。高频进度突发存在广播事件丢弃，已记录为 V1.0 后续优化。
- [~] M7-03 已增加每次 Sidecar 调用的双文件 SHA-256 验证、暂存提交、重解析点/磁盘空间防护与对应单元测试；加密创建后的完整性测试现会复用仅在内存中的密码。7-Zip 进程在 Windows 使用无控制台创建标志；完整恶意样本、密码日志与临时文件矩阵待补。
- [~] M7-04 已本地生成并校验 Windows x64 NSIS、校验和和 Sidecar；本次已用当前用户开发证书重新生成 NSIS 安装包并验证 Shell DLL 开发签名。MSI 与便携 ZIP 暂不作为当前公开发行资产；尚未接入真实签名凭据或生成正式公开资产。
- [~] M7-05 已完成 README、隐私说明、卸载说明、已知问题、变更记录、RC 验收记录和仅手动触发的 GitHub NSIS 工作流；普通提交只运行 CI，只有受信任签名验证通过的手动发布才会创建公开 Release。
- [~] M7-06 已建立 RC1 验收记录与已知问题边界；尚未执行完整 V1.0 总验收。

### RC1 发布前剩余动作（按优先级）

- [~] R1-01 已提供 Windows Sandbox 隔离验收配置、自动安装/卸载记录脚本和结果目录，并已在本机 Windows 11 完成 NSIS 安装、Shell 包注册及真实 Explorer 菜单可见性验收；仍须在干净 Sandbox 环境执行安装、升级、卸载、文件关联清理和 DPI 100%/150%/200% 验收。
- [~] R1-02 已完成创建/解压、取消、错误密码、覆盖冲突、文件占用、临时清理及对应自动化回归；压缩炸弹、磁盘不足、链接和完整恶意路径矩阵仍待完成。
- [x] R1-03 已记录 `artifacts/performance/rc1-baseline.json` 原始基线数据；第三方压缩软件交叉兼容性按 RC1 范围豁免，不作为发布门禁。
- [ ] R1-04 取得受信任代码签名 PFX 后运行 `scripts/configure-github-signing.ps1` 配置 `WINDOWS_PFX_BASE64`、`WINDOWS_PFX_PASSWORD` 和 `QZIP_WINDOWS_PUBLISHER`，验证手动发布的受信任签名与失败关闭。
- [ ] R1-05 完成 RC 验收记录签字后，从 GitHub Actions 以 `publish` 模式手动创建对应标签和公开 prerelease。

### V1.0 稳定版剩余阻塞项

- [ ] 完成 M2 的可取消深度扫描、底部任务条和创建流程真实 UI 自动化验收。
- [ ] 完成 M3 的风险包、磁盘不足、链接、恶意路径和批量密码包专项矩阵。
- [ ] 完成 M4 的虚拟列表、暗夜/窄窗口、键盘操作和选择状态验收。
- [ ] 完成任务进度事件节流，并复测突发事件不丢失终态和关键阶段。
- [ ] 完成 Windows V1.0 功能、体验、安全与发布总验收后再发布稳定版。

## V1.1 Backlog：跨平台扩展

- [ ] V1.1-01 提供 macOS Apple Silicon/Intel Sidecar、DMG、签名、公证和 Gatekeeper 验收。
- [ ] V1.1-02 提供 Finder Quick Action、Command 快捷键和 macOS 文件关联/卸载验证。
- [ ] V1.1-03 提供 Linux Sidecar、AppImage、MIME、Desktop Entry 和卸载验证。
- [ ] V1.1-04 完成 Wayland/X11、平台路径、权限、链接与桌面环境兼容性测试。
- [ ] V1.1-05 建立对应平台的签名、校验和、许可、构建和发布流水线。

## 持续性工作

- [ ] Q-01 每次改变领域模型，同时更新 Rust、TypeScript、Tauri DTO、文档和测试。
- [ ] Q-02 每个核心流程都覆盖加载、空、成功、失败、禁用以及浅色/暗夜状态。
- [ ] Q-03 PR 持续执行格式、类型、Lint、前端测试、Rust 测试、Clippy、构建、许可证和密钥检查。
- [ ] Q-04 对新增依赖记录用途与许可证；对 Sidecar 记录版本、来源、修改情况和对应源码。
- [ ] Q-05 未实测前不得对外承诺性能指标；发现安全风险时优先阻断功能并补测试。

## 完成判定

一个 TODO 仅在以下条件全部满足时标记完成：功能行为符合需求、核心逻辑有测试、错误提示可操作、密码和敏感路径不泄露、主题可用、无明显控制台错误，并通过所属里程碑的验收项。
