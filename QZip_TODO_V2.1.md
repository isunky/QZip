# 轻压 · QZip 实施 TODO（V2.1）

> 需求来源：`QZip_PRD_Requirements_V2.1.md` 与原始基线 `QZip_PRD_V2.1.md`。
>
> 规则：按里程碑推进；未通过对应验收不得提前开发后续功能；所有涉及密码、路径、Sidecar、任务状态或数据契约的任务必须附测试。

## 当前状态

- [x] **M0** 仓库、设计系统与静态首页已完成并通过 `pnpm check`、Tauri Debug 构建和设计 QA。
- [x] **M1** 后端技术验证已实现并通过 7Z/ZIP/RAR、Unicode、密码、损坏包、取消和打包验证。
- [ ] **M2–M7** 尚未实施。

## M0：仓库和设计系统（完成）

- [x] 建立 pnpm Monorepo、Rust Workspace、Tauri 2 桌面壳与五个领域 crate。
- [x] 配置 `QZip`、`app.qzip.desktop`、Node/Rust 工具链、许可证与 GitHub Actions。
- [x] 建立 `@qzip/ui` Token、基础组件、浅色/暗夜和 6 套主题。
- [x] 实现 PIC 参考图风格的首页、标题栏、任务空态、设置面板、主题切换与最小窗口布局。
- [x] 生成应用图标，完成首页视觉对照报告。

**验收记录**：`design-qa.md`；`pnpm check`；`target/debug/qzip-desktop.exe`。

## M1：后端技术验证

### M1-A 领域与后端接口

- [x] M1-01 在 `archive-core` 定义格式、预设、冲突策略、操作、任务状态、请求/结果、错误和能力模型。
- [x] M1-02 定义 `ArchiveBackend` 与 `BackendRegistry`；确保 `archive-core` 不依赖 Tauri 或 7-Zip。
- [x] M1-03 定义能力、进度、错误 DTO 与对应 TypeScript 类型；M2/M3 再接入创建/解压 Tauri Command。
- [x] M1-04 为领域模型、路径安全、参数映射、输出解析、错误映射和取消行为编写自动化测试。

### M1-B SevenZipCliBackend

- [x] M1-05 建立 Sidecar 目录、目标三元组命名、版本记录、官方 MSI/源码来源、SHA-256 和许可证文件。
- [x] M1-06 实现 Sidecar 定位与启动前能力/版本探测；Windows 打包前校验目标二进制与源码存在。
- [x] M1-07 以 `Command::new(...).arg(...).args(...)` 实现进程启动；禁止 Shell 字符串。
- [x] M1-08 建立集中式版本检查、参数映射、输出解析、列表解析和错误映射。
- [x] M1-09 已实现创建 7Z/ZIP、解压 7Z/ZIP/RAR、列表、测试和取消，并完成真实样本验证。
- [x] M1-10 建立开发者专用 `qzip-cli` 受控验证入口。

### M1-C M1 验收

- [x] M1-11 中文、空格和 Unicode 路径成功；所有操作不经 Shell。
- [x] M1-12 密码、完整命令和敏感文件清单不进入日志/事件/历史。
- [x] M1-13 成功、密码错误、损坏包、磁盘不足、未知退出码均映射为结构化错误。
- [x] M1-14 参数映射、输出解析和取消行为具有自动化测试。

## M2：创建压缩包

- [ ] M2-01 实现普通文件/文件夹拖拽识别及可取消输入扫描。
- [ ] M2-02 实现创建压缩包页面：输入摘要、名称、位置、格式、预设、密码与更多设置。
- [ ] M2-03 实现默认命名、输出位置、序号避重名和“不直接覆盖”规则。
- [ ] M2-04 基于后端能力实现格式与选项动态禁用；专家参数渐进展示。
- [ ] M2-05 通过 Command 创建任务，接入底部任务条与实时事件；页面不等待任务完成。
- [ ] M2-06 实现删除源文件二次确认及压缩后测试选项。
- [ ] M2-07 测试 7Z/ZIP 创建、两次点击开始、取消和 UI 不阻塞。

## M3：快速解压和安全

### M3-A 解压体验

- [ ] M3-01 实现压缩包格式识别、摘要、快速解压页和同名目录默认输出。
- [ ] M3-02 实现冲突策略、密码输入/原地重试、查看内容和任务创建。
- [ ] M3-03 支持单包快速解压、多包多任务解压与混合拖拽处理选择。

### M3-B 安全服务

- [ ] M3-04 实现路径规范化、绝对路径/盘符/UNC/Zip Slip/保留名/尾随空格句点检查。
- [ ] M3-05 实现符号链接和硬链接默认拒绝策略及最终输出路径边界校验。
- [ ] M3-06 实现条目数、尺寸、深度、压缩比、磁盘空间等压缩炸弹限制和风险提示。
- [ ] M3-07 实现取消后的临时/未完成文件清理与无法清理告警。
- [ ] M3-08 覆盖密码错误、风险包、冲突覆盖和取消的安全/集成测试。

## M4：压缩包浏览

- [ ] M4-01 实现浏览页工具栏、面包屑、搜索、状态栏和默认列。
- [ ] M4-02 实现目录导航、多选、即时过滤、属性查看、部分解压和完整性测试。
- [ ] M4-03 引入虚拟列表与极大压缩包分批加载；验证搜索和滚动不阻塞。
- [ ] M4-04 完成暗夜与窄窗口的可读性、键盘和选择状态测试。

## M5：任务中心

- [ ] M5-01 实现 `task-runtime` 队列、状态机、进度节流、取消、历史和重试参数克隆。
- [ ] M5-02 实现进行中、已完成、失败视图及任务卡详情。
- [ ] M5-03 实现取消、重试、打开结果、打开位置、清理已完成和技术详情入口。
- [ ] M5-04 禁止任务重试复用密码、临时路径或原进程 ID。
- [ ] M5-05 覆盖非法状态跳转、多任务并发、失败修复提示和清理行为。

## M6：设置与系统集成

- [ ] M6-01 实现 `AppSettings` 持久化、默认值、迁移和“不得持久化密码”测试。
- [ ] M6-02 完成所有设置页：常规、压缩、解压、外观、隐私安全、更新、关于。
- [ ] M6-03 持久化主题、缩放、密度、减少动态与压缩/解压偏好。
- [ ] M6-04 实现单实例、文件关联、双击打开、定位路径和系统通知。
- [ ] M6-05 实现 Windows 右键高频操作；评估并实现 macOS Finder 与 Linux MIME/Desktop Entry。
- [ ] M6-06 验证安装、卸载、关联注册/清理和各平台路径行为。

## M7：发布候选

- [ ] M7-01 建立兼容样本库：7-Zip、WinRAR、Bandizip、PeaZip、Keka、系统 ZIP、tar/gzip/xz；样本不含隐私或版权受限数据。
- [ ] M7-02 完成性能基线：启动、首页交互、空闲内存、大列表、进度事件和异常恢复。
- [ ] M7-03 完成安全测试矩阵：路径、链接、炸弹、注入、恶意文件名、密码日志与临时文件。
- [ ] M7-04 完成三平台构建、安装包、Sidecar 校验、校验和、第三方许可和依赖溯源。
- [ ] M7-05 完成 README、隐私说明、卸载说明、已知问题、发行说明和 GitHub Release 流程。
- [ ] M7-06 逐项执行 V1.0 功能、体验、安全和发布总验收。

## 持续性工作

- [ ] Q-01 每次改变领域模型，同时更新 Rust、TypeScript、Tauri DTO、文档和测试。
- [ ] Q-02 每个核心流程都覆盖加载、空、成功、失败、禁用以及浅色/暗夜状态。
- [ ] Q-03 PR 持续执行格式、类型、Lint、前端测试、Rust 测试、Clippy、构建、许可证和密钥检查。
- [ ] Q-04 对新增依赖记录用途与许可证；对 Sidecar 记录版本、来源、修改情况和对应源码。
- [ ] Q-05 未实测前不得对外承诺性能指标；发现安全风险时优先阻断功能并补测试。

## 完成判定

一个 TODO 仅在以下条件全部满足时标记完成：功能行为符合需求、核心逻辑有测试、错误提示可操作、密码和敏感路径不泄露、主题可用、无明显控制台错误，并通过所属里程碑的验收项。
