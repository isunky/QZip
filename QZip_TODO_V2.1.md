# 轻压 · QZip 实施 TODO（V2.1）

> 需求来源：`QZip_PRD_Requirements_V2.1.md` 与原始基线 `QZip_PRD_V2.1.md`。
>
> 规则：按里程碑推进；未通过对应验收不得提前开发后续功能；所有涉及密码、路径、Sidecar、任务状态或数据契约的任务必须附测试。
>
> 状态说明：`[x]` 已完成并通过对应验收；`[~]` 已实现第一版，仍有验收或范围缺口；`[ ]` 未实施。

## 当前状态

- [x] **M0** 仓库、设计系统与静态首页已完成并通过 `pnpm check`、Tauri Debug 构建和设计 QA。
- [x] **M1** 后端技术验证已实现并通过 7Z/ZIP/RAR、Unicode、密码、损坏包、取消和打包验证。
- [~] **M2–M5** 已实施第一版：创建、快速解压与安全预检、压缩包浏览、任务队列和任务中心已接入桌面端；待补齐对应的完整验收、边界流程与自动化测试。
- [~] **M6** 已完成设置核心、设置页、单实例路由及 Windows Shell 集成代码；安装注册与跨平台验证待完成。
- [~] **M7** 已实施 RC1 的安全写入、Sidecar 完整性、Windows 发布物组装与严格签名门禁；兼容性样本、性能基线、真实安装验证及 RC2 跨平台范围仍未完成。

## M0：仓库和设计系统（完成）

- [x] 建立 pnpm Monorepo、Rust Workspace、Tauri 2 桌面壳与五个领域 crate。
- [x] 配置 `QZip`、`app.qzip.desktop`、Node/Rust 工具链、许可证与 GitHub Actions。
- [x] 建立 `@qzip/ui` Token、基础组件、浅色/暗夜和 6 套主题。
- [x] 实现 PIC 参考图风格的首页、标题栏、任务空态、设置面板、主题切换与最小窗口布局。
- [x] 生成应用图标，完成首页视觉对照报告。

**验收记录**：`design-qa.md`；`pnpm check`；`target/debug/qzip-desktop.exe`。

## M1：后端技术验证

### M1-A 领域与后端接口

- [x] M1-01 在 `archive-core` 定义格式、预设、冲突策略、操作、任务状态、请求/结果、错误和能力模型。
- [x] M1-02 定义 `ArchiveBackend` 与 `BackendRegistry`；确保 `archive-core` 不依赖 Tauri 或 7-Zip。
- [x] M1-03 定义能力、进度、错误 DTO 与对应 TypeScript 类型；M2/M3 再接入创建/解压 Tauri Command。
- [x] M1-04 为领域模型、路径安全、参数映射、输出解析、错误映射和取消行为编写自动化测试。

### M1-B SevenZipCliBackend

- [x] M1-05 建立 Sidecar 目录、目标三元组命名、版本记录、官方 MSI/源码来源、SHA-256 和许可证文件。
- [x] M1-06 实现 Sidecar 定位与启动前能力/版本探测；Windows 打包前校验目标二进制与源码存在。
- [x] M1-07 以 `Command::new(...).arg(...).args(...)` 实现进程启动；禁止 Shell 字符串。
- [x] M1-08 建立集中式版本检查、参数映射、输出解析、列表解析和错误映射。
- [x] M1-09 已实现创建 7Z/ZIP、解压 7Z/ZIP/RAR、列表、测试和取消，并完成真实样本验证。
- [x] M1-10 建立开发者专用 `qzip-cli` 受控验证入口。

### M1-C M1 验收

- [x] M1-11 中文、空格和 Unicode 路径成功；所有操作不经 Shell。
- [x] M1-12 密码、完整命令和敏感文件清单不进入日志/事件/历史。
- [x] M1-13 成功、密码错误、损坏包、磁盘不足、未知退出码均映射为结构化错误。
- [x] M1-14 参数映射、输出解析和取消行为具有自动化测试。

## M2：创建压缩包

- [ ] M2-01 实现普通文件/文件夹拖拽识别及可取消输入扫描。
- [~] M2-02 已实现创建压缩包页面：输入列表、名称/位置、7Z/ZIP/TAR/TAR.GZ/TAR.XZ、压缩预设、密码和完整性测试选项；待补“更多设置”与完整交互验收。
- [~] M2-03 已在运行时拒绝覆盖既有输出、拒绝输入目录内递归输出，并以临时文件提交；默认命名、输出位置与序号避重名 UI 待补。
- [ ] M2-04 基于后端能力实现格式与选项动态禁用；专家参数渐进展示。
- [~] M2-05 已通过 Tauri Command 创建任务并订阅实时事件，创建页不等待任务完成；待补底部任务条和 UI 自动化验收。
- [~] M2-06 已实现压缩后测试选项；删除源文件二次确认尚未实现。
- [ ] M2-07 测试 7Z/ZIP 创建、两次点击开始、取消和 UI 不阻塞。

## M3：快速解压和安全

### M3-A 解压体验

- [~] M3-01 已实现压缩包格式识别、摘要和快速解压页；同名目录默认输出待补。
- [~] M3-02 已实现冲突策略、密码输入、查看内容入口和任务创建；密码错误后的原地重试待补全至任务卡流程。
- [ ] M3-03 支持单包快速解压、多包多任务解压与混合拖拽处理选择。

### M3-B 安全服务

- [x] M3-04 已实现并测试路径规范化、绝对路径/盘符/UNC/Zip Slip/保留名/尾随空格句点检查。
- [x] M3-05 已在预检中拒绝符号链接和硬链接；最终输出路径会拒绝重解析点，并先解压到同卷暂存目录后安全合并。
- [x] M3-06 已实现条目数、尺寸、深度和压缩比限制及一次性风险确认，并在解压前检查可用磁盘空间。
- [~] M3-07 创建和解压的取消/失败均清理受控临时输出；无法清理告警与真实 7-Zip 集成回归待补。
- [~] M3-08 已覆盖递归输出、既有输出及重命名冲突的运行时单元测试；密码、风险包、覆盖和取消的完整集成矩阵待补。

## M4：压缩包浏览

- [~] M4-01 已实现浏览页工具栏、面包屑、搜索和默认列；状态栏待补。
- [~] M4-02 已实现多选与即时过滤；目录导航、属性查看、将所选项传入部分解压和完整性测试待补。
- [~] M4-03 后端目录接口已支持每页最多 500 项的分页；前端虚拟列表、追加加载与大包性能验收待补。
- [ ] M4-04 完成暗夜与窄窗口的可读性、键盘和选择状态测试。

## M5：任务中心

- [~] M5-01 已实现最多 2 并发的 `task-runtime`、状态机、进度事件、取消、100 条本地历史和重试参数克隆；状态机与并发自动化测试待补。
- [~] M5-02 已实现进行中、已完成、失败的任务视图与任务卡基础信息；详情面板待补。
- [~] M5-03 已实现取消、重试和清理已完成；打开结果、打开位置及技术详情入口待接入 UI。
- [~] M5-04 已确保密码不进入任务快照/本地历史，重启后的历史记录不可重试；临时路径与进程 ID 的专项测试待补。
- [ ] M5-05 覆盖非法状态跳转、多任务并发、失败修复提示和清理行为。

## M6：设置与系统集成

- [~] M6-01 实现 `AppSettings` 持久化、默认值、迁移和“不得持久化密码”单元测试；需补充损坏 Store 文件端到端测试。
- [x] M6-02 完成所有设置页：常规、压缩、解压、外观、隐私安全、更新、关于。
- [x] M6-03 持久化主题、缩放、密度、减少动态与压缩/解压偏好。
- [~] M6-04 实现单实例、文件关联声明、双击打开路由和系统通知；待安装环境实测关联注册。
- [~] M6-05 实现 Windows 右键高频操作 DLL 和受限 token 交接；macOS Finder 与 Linux MIME/Desktop Entry、稀疏 MSIX 注册待完成。
- [ ] M6-06 验证安装、卸载、关联注册/清理和各平台路径行为。

## M7：发布候选

- [~] M7-01 已建立合规兼容样本清单与 SHA-256 校验脚本；7-Zip、WinRAR、Bandizip、PeaZip、Keka、系统 ZIP、tar/gzip/xz 的实际可分发样本及生产工具版本仍待补齐。
- [ ] M7-02 完成性能基线：启动、首页交互、空闲内存、大列表、进度事件和异常恢复。
- [~] M7-03 已增加每次 Sidecar 调用的双文件 SHA-256 验证、暂存提交、重解析点/磁盘空间防护与对应单元测试；完整恶意样本、密码日志与临时文件矩阵待补。
- [~] M7-04 已完成 Windows x64 RC 资产组装、校验和、portable Sidecar 复核和可信 Authenticode 签名门禁；尚未实测签名凭据、未生成正式资产，macOS/Linux 与 SBOM 仍属 RC2/后续工作。
- [~] M7-05 已完成 README、隐私说明、卸载说明、已知问题、变更记录、RC 验收记录和标签触发的 GitHub prerelease 工作流；必须配置受信任签名秘密后才会创建公开 Release。
- [~] M7-06 已建立 RC1 验收记录与已知问题边界；尚未执行完整 V1.0 总验收。

## 持续性工作

- [ ] Q-01 每次改变领域模型，同时更新 Rust、TypeScript、Tauri DTO、文档和测试。
- [ ] Q-02 每个核心流程都覆盖加载、空、成功、失败、禁用以及浅色/暗夜状态。
- [ ] Q-03 PR 持续执行格式、类型、Lint、前端测试、Rust 测试、Clippy、构建、许可证和密钥检查。
- [ ] Q-04 对新增依赖记录用途与许可证；对 Sidecar 记录版本、来源、修改情况和对应源码。
- [ ] Q-05 未实测前不得对外承诺性能指标；发现安全风险时优先阻断功能并补测试。

## 完成判定

一个 TODO 仅在以下条件全部满足时标记完成：功能行为符合需求、核心逻辑有测试、错误提示可操作、密码和敏感路径不泄露、主题可用、无明显控制台错误，并通过所属里程碑的验收项。
