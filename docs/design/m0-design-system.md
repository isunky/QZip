# M0 design system

The design system uses --qzip- CSS variables. Base mode tokens, accent tokens,
and component tokens are layered so component CSS never hard-codes a theme
colour. Mint is the default accent; all six PRD accents support light and dark
modes. The visual benchmark is the image set in PIC.
