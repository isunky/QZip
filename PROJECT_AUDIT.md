# QZip 项目审查与改进建议

审查基线：`main` 分支，提交 `037d0b6`，版本 `1.1.2`。

本次已运行 `pnpm check`：ESLint、TypeScript、前端测试、Web 构建、Rust 格式检查、Clippy 和 Rust 测试均通过。以下问题来自额外的边界验证与代码审查；其中标记“已复现”的问题有可重复的实际输入样本。

## 优先处理

### 1. 压缩包文件列表被截断，中文文件名可能使后端崩溃

**等级：高｜状态：已修复**

7-Zip 的完整文件清单当前和诊断输出共用 64 KB 上限。一个含 1,200 个文件的 ZIP 被读取后只返回 249 项，但调用仍报告成功，导致浏览列表、文件数量、预计解压大小和风险预检均可能不完整。

当输出在 UTF-8 中文字符中间被截断时，字符串按字节切片会触发 Rust panic。已复现 CLI 以退出码 `101` 结束，并出现 `end byte index ... is not a char boundary`。

建议：

- 将清单输出与诊断输出分离；完整清单必须流式解析，诊断信息才允许限长。
- 截断文本必须按 UTF-8 字符边界处理，不能按任意字节索引切片。
- 增加 1,000+ 条目、超长名称、中文名称和边界截断回归测试；测试必须校验实际条目数与源归档一致。

**修复：** 清单标准输出现在按行流式交给 `SevenZipListParser`，不再受 64 KB 诊断上限影响；stderr 继续单独限长用于错误诊断，并按 UTF-8 字符边界截断。新增 1,500 条目解析单测和 1,250 个中文文件的真实 7-Zip 集成回归。

相关代码：[archive-sevenzip/src/lib.rs](crates/archive-sevenzip/src/lib.rs:39)、[archive-sevenzip/src/lib.rs](crates/archive-sevenzip/src/lib.rs:815)。

### 2. 覆盖解压失败时可能丢失原文件

**等级：高｜状态：已修复**

覆盖模式会先删除目标文件，再移动解压后的新文件。若移动过程失败，例如文件锁定、权限变化或磁盘异常，原文件已被删除，用户会丢失已有数据。合并至已有目录时也存在部分文件已写入、任务整体失败的情况。

建议：

- 对冲突目标进行完整预检查，避免任务执行到中途才发现不能写入。
- 用临时备份和原子替换完成单文件覆盖，确保失败时保留旧文件。
- 对目录合并定义明确的提交策略：可回滚，或在任务结果中标识“部分完成”并给出准确文件列表。
- 增加“替换阶段失败仍保留旧内容”的集成测试。

**修复：** 覆盖解压现在先将原文件移入同目录临时备份，再替换为暂存文件；替换失败时会自动恢复原文件。已有目录的合并使用变更日志，任一步失败都会按逆序回滚已创建和已替换内容；备份清理失败也会保留备份以优先保证原文件安全。新增后续冲突触发整体回滚的回归测试，以及 Windows 源文件锁定导致替换失败仍恢复原文件的测试。

相关代码：[task-runtime/src/lib.rs](crates/task-runtime/src/lib.rs:922)。

### 3. 浏览 TAR.GZ/TAR.XZ 时会先完整解包到系统临时目录

**等级：高｜状态：已修复**

查看 `.tar.gz` 或 `.tar.xz` 内容时，程序会先把内部 TAR 展开到临时目录，再读取清单。这意味着用户仅点击“查看”就可能占用大量系统盘空间；展开阶段没有面向界面的取消控制，也未在展开前应用完整的空间和大小限制。

建议：

- 展开前估算磁盘空间并应用最大临时文件限制。
- 将展开过程纳入可取消任务，并在界面显示进度。
- 在同一个会话中缓存并复用已验证的内部 TAR，避免浏览、测试、解压重复展开。
- 对超大 `.tar.gz`、取消操作和临时目录清理建立回归测试。

**修复：** 浏览复合 TAR 时改为启动两个受控的 7-Zip 子进程：外层 gzip/xz 使用 `x -so` 输出解压流，内层 TAR 列表器通过管道读取并解析，浏览过程不再把内部 TAR 写入系统临时目录。取消请求会同时终止两个子进程并回收管道；失败路径统一回收子进程和管道资源。新增 Windows 真实 TAR.XZ 浏览回归测试，验证能读取条目且不会创建 `qzip-expand-*` 临时目录；现有 TAR.GZ/TAR.XZ 集成回归继续覆盖两种格式。

相关代码：[archive-sevenzip/src/lib.rs](crates/archive-sevenzip/src/lib.rs:376)。

### 4. 任务事件缓冲区溢出后，界面事件转发可能永久停止

**等级：高｜状态：已修复**

任务运行时使用固定容量 256 的广播通道。接收端使用 `while let Ok(...)`；一旦收到 `Lagged` 溢出错误，循环直接退出，之后的完成或失败状态不能继续转发到前端。现有性能报告也记录过进度事件丢失。

建议：

- 显式处理 `Lagged`，记录丢失事件后继续接收。
- 接收端发生溢出时主动重新读取任务快照，使 UI 回到真实状态。
- 对进度事件做节流或合并，终态事件使用不会被进度淹没的可靠通道。

**修复：** 桌面端任务事件转发不再使用 `while let Ok(...)` 静默退出；现在会明确处理 `Lagged` 并从任务管理器读取最新快照，为每个任务发送 `task.resync` 同步事件后继续监听，只有 `Closed` 才结束循环。前端收到同步事件时仍会更新任务列表，但不会重复触发完成/失败 Toast 或系统通知。新增广播缓冲区溢出后接收器继续可用的回归测试。

相关代码：[task-runtime/src/lib.rs](crates/task-runtime/src/lib.rs:132)、[desktop/lib.rs](apps/desktop/src-tauri/src/lib.rs:1699)。

## 发布前应处理

### 5. 签名凭据变量不一致，可信签名构建可能错误降级

**等级：中｜状态：代码审查确认**

GitHub Actions 提供 `WINDOWS_PFX_PASSWORD`，构建脚本要求 `QZIP_WINDOWS_PFX_PASSWORD`，工作流没有将前者映射为后者。配置了有效证书后，可信签名构建仍可能因缺失变量失败，并回退为 `unsigned-degraded`。

建议：统一变量名，或在签名准备步骤写入 `QZIP_WINDOWS_PFX_PASSWORD`；同时在 CI 中分别验证可信签名路径和无签名降级路径。

相关代码：[release.yml](.github/workflows/release.yml:131)、[bundle-windows.ps1](scripts/bundle-windows.ps1:40)。

### 6. `.tgz`、`.txz` 别名没有按复合 TAR 格式处理

**等级：中｜状态：已修复**

`.tgz` 和 `.txz` 现在在桌面层、核心格式枚举、文件选择器以及 7-Zip 后端复合包分流中分别映射到 `TarGz` 和 `TarXz`。浏览会继续通过流式 TAR 管道读取内部文件，测试和解压也会走复合包路径，不再把别名文件当作普通 GZip/XZ 文件展示内部 TAR。

回归测试覆盖 `.tgz/.txz` 的格式解析、桌面识别和 7-Zip 复合包分流，并保留普通 `.gz/.xz` 不进入 TAR 分流。

相关代码：[archive-sevenzip/src/lib.rs](crates/archive-sevenzip/src/lib.rs:859)、[desktop/lib.rs](apps/desktop/src-tauri/src/lib.rs:885)、[archive-core/src/lib.rs](crates/archive-core/src/lib.rs:383)。

### 7. 自动更新仍是占位能力

**等级：中｜状态：已修复（GitHub Release 检查）**

设置页的“检查更新”现在会在 10 秒超时内查询 GitHub 最新稳定 Release，校验返回的版本号并与当前版本比较。检查成功后会明确显示“已是最新版本”或“发现新版本”，后者提供 GitHub 下载页入口；网络失败、HTTP 异常和无效响应会以可恢复错误返回，不再用 `ready/unconfigured` 假装完成检查。

当前仍保留启动自动检查开关但保持禁用，待发布渠道配置 Tauri updater 的签名元数据后再启用自动下载、签名校验和安装流程；手动检查与下载页跳转已可用于正式版本。

相关代码：[desktop/lib.rs](apps/desktop/src-tauri/src/lib.rs:1262)、[settingsClient.ts](apps/desktop/src/lib/settingsClient.ts:22)、[SettingsPage.tsx](apps/desktop/src/features/settings/SettingsPage.tsx:91)。

## 稳定性与维护性改进

### 8. 压缩包会话未在前端切换页面时释放

**等级：中｜状态：已修复**

桌面端现在跟踪当前活动会话，并在返回首页、切换到任务中心或设置页、完成解压任务、切换到新的压缩包，以及进入批量解压流程时释放旧会话。浏览压缩包返回解压页会保留同一会话以支持继续操作，关闭浏览页则沿用统一清理路径。批量解压对每个已准备会话使用 `finally` 关闭，即使输出建议或任务创建失败也不会泄漏；关闭失败按 best-effort 处理，不阻塞页面导航。

回归测试覆盖页面切换释放和批量解压后的会话关闭。

相关代码：[archiveClient.ts](apps/desktop/src/lib/archiveClient.ts:19)、[App.tsx](apps/desktop/src/app/App.tsx:116)、[ArchivePages.tsx](apps/desktop/src/features/archive/ArchivePages.tsx:646)。

### 9. 任务历史写入没有串行化，也没有按最近更新时间保留

**等级：中｜状态：已修复**

任务历史现在由写入锁串行保护，快照、序列化和文件替换在同一写入临界区内完成；临时文件使用唯一名称，避免并发任务互相覆盖。历史记录按 `updated_at` 倒序（再按创建时间和任务 ID 稳定排序）后保留最近 100 条，加载旧历史时也会先重新排序再截断。写入完成后同步文件内容，Unix 使用原子 `rename`，Windows 使用备份—替换—失败恢复流程，原文件不会因替换失败而被删除；目录创建、序列化、同步和替换错误会记录到日志。

回归测试覆盖最近 100 条保留、旧历史重新排序以及并发写入后 JSON 完整性。

相关代码：[task-runtime/src/lib.rs](crates/task-runtime/src/lib.rs:686)。

### 10. 大型核心文件承担过多职责

**等级：中**

`ArchivePages.tsx`、桌面端 `lib.rs` 与 `task-runtime/lib.rs` 均在 1,000 行以上，分别混合了 UI、会话、文件预览、系统集成、任务状态、持久化和安全写入逻辑。功能继续增加后，回归风险会快速上升。

建议：按职责拆分为浏览会话、预览、系统集成、任务状态机、历史存储等模块；优先在修复高优问题时顺带抽离相邻代码。

## 建议实施顺序

1. 修复清单截断与 UTF-8 panic，并补大包、中文文件名测试。
2. 修复覆盖解压的原文件保护和目录提交策略。
3. 修复事件溢出后的 UI 状态恢复。
4. 统一签名密码环境变量，验证发布工作流两条路径。
5. 修复 TGZ/TXZ 识别、释放会话、完善任务历史。
6. 再推进自动更新和大文件模块拆分。

## 已有基础

项目已有较好的基础：核心压缩能力与 UI 分层清晰，Sidecar 校验、路径穿越防护、临时目录清理、文件关联图标和无签名发布降级机制均已具备。下一阶段的重点应从“继续加功能”转为“让大包、异常、升级和发布场景足够稳定”。
